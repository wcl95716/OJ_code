#include <bits/stdc++.h>
using namespace std;

#include <algorithm>
#include <cstdio>
#include <cstring>
#include <iostream>
#include <vector>
using namespace std;
class Solution {
public:
  int electricCarPlan(vector<vector<int>> &paths, int cnt, int start, int end,
                      vector<int> &charge) {}

  struct node {
    int x, y, distance;
  };

  void dfs(int city, int mp, vector<vector<node>>) {

    for (int i = 0; i < paths[city].size(); ++i) {
      int nextCity = paths[city][i][0];
    }
  }
};

int main() {

#if defined(__APPLE__)
  freopen("./slyar.in", "r+", stdin);
  freopen("./slyar.out", "w+", stdout);
#endif
}

int main() {
#if defined(__APPLE__)
  freopen("./slyar.in", "r+", stdin);
  freopen("./slyar.out", "w+", stdout);
#endif
  run();
}